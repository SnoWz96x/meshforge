import { ComingSoon } from "@/components/coming-soon";

export default function WorkflowPage() {
  return (
    <ComingSoon
      title="Workflow"
      phase="Futuro — editor de nós"
      desc="Editor visual de nós para montar pipelines customizados. O pipeline completo (SDXL → Hunyuan3D → textura → otimizar → exportar) JÁ funciona em 1 clique na tela Gerar (Pacote completo); esta tela seria o editor visual avançado. Ainda não implementado — registrado em REQUIREMENTS_TRACKING.md."
    />
  );
}
