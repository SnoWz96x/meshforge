import { ComingSoon } from "@/components/coming-soon";

export default function WorkflowPage() {
  return (
    <ComingSoon
      title="Workflow"
      phase="Fase 5 — orquestração"
      desc="Visualização do pipeline completo (SDXL → Hunyuan3D → Blender → Export) com status por etapa. Ainda não implementado — registrado em REQUIREMENTS_TRACKING.md."
    />
  );
}
